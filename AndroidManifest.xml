<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="zul.android.chat"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="14"
        android:targetSdkVersion="22" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name="zul.android.chat.LogInPage"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="zul.android.chat.RegisterPage" />
        <activity android:name="zul.android.chat.HomeActivity" />
        <activity android:name="zul.android.chat.Notification" />
        <activity
            android:name="CommonActivity"
            android:parentActivityName="zul.android.chat.HomeActivity" >
        </activity>
    </application>

</manifest>